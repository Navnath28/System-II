
        mov eax, 10
    add eax, ebx
        sub ecx, edx
    push esi
        pop edi
    mov ebp, 25
        mov esp, eax
    add ebx, 100
        sub esi, eax
    push 20
        jmp start
    mov eax, [ebx]
        mov ecx, [edx]

